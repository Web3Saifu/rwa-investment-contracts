# 業務フロー図（Markdown化）

---

## 1. 商品登録

```mermaid
flowchart LR
    A[管理者] --> B[商品画面]
    B --> C[商品登録処理]
    C --> D[(商品管理DB)]
    C --> E[商品登録]
    E -.-> F[(商品情報)]
    E -->|deploy| G[(NFT情報)]

    subgraph FRONT[フロントシステム]
      B
      C
      D
    end

    subgraph CONTRACT[Web3小口投資プラットフォーム（contract）]
      E
      F
    end

    subgraph NFTC[商品NFT（contract）]
      G
    end
```

### 補足
- 管理者が商品情報を入力
- フロント側で商品登録処理を実施
- 商品管理DBへ保存
- コントラクト側で商品登録を行い、NFT コントラクトを deploy

---

## 2. 出資申込（日本円）→ NFT 発行

```mermaid
flowchart LR
    I[出資者] -->|出資申込| P[商品画面]
    P --> J[投資申込画面<br/>（日本円入金）]
    J --> K[(申込情報DB)]

    I -->|入金| B[銀行]
    B -->|入金確認| A[管理者]

    A --> M[入金申請管理画面]
    M --> N[NFT発行]
    N -->|NFT発行| W[出資者 Wallet]

    K -.->|入金先情報・管理番号連携| I

    subgraph FRONT[フロントシステム]
      P
      J
      K
      M
    end

    subgraph CONTRACT[Web3小口投資プラットフォーム（contract）]
      N
    end
```

### 入金確認情報
- 入金者（管理番号・名前）
- 入金金額

### 補足
- 出資者は商品画面から日本円入金の申込を行う
- 申込情報はフロント側DBに保存
- 出資者は銀行へ入金
- 管理者が着金確認後、管理画面から NFT を発行
- NFT は出資者ウォレットへ発行

---

## 3. 出資申込（USDT）→ NFT 発行

```mermaid
flowchart LR
    I[出資者] -->|出資申込| P[商品画面]
    P --> U[投資申込画面<br/>（USDT入金）]
    U --> C[出資申込]
    C --> N[NFT発行]
    N -->|NFT送信| W[出資者 Wallet]

    subgraph FRONT[フロントシステム]
      P
      U
    end

    subgraph CONTRACT[Web3小口投資プラットフォーム（contract）]
      C
      N
    end
```

### 補足
- 出資者が商品画面から USDT で出資申込
- コントラクト側で出資申込を処理
- NFT を発行して出資者ウォレットへ発行

---

## 4. 利益分配・満期処理

```mermaid
flowchart LR
    CL[定期実行プラットフォーム<br/>（chainlink）] --> T[利益分配・満期処理判定]

    A[管理者] -->|USDT補充| D[USDT入金]
    AW[管理者 Wallet] -.->|USDT送信| P[(USDTプール)]
    D -->|USDT送金| P

    T --> X[利益分配<br/>満期処理]
    X -->|送信指示| P

    P -->|USDT送付| W1[出資者 Wallet]
    P -->|USDT送付| W2[出資者 Wallet]
    P -->|USDT送付| W3[出資者 Wallet]

    A -.->|送信指示| D

    subgraph CHAINLINK[定期実行プラットフォーム（chainlink）]
      CL
    end

    subgraph CONTRACT[Web3小口投資プラットフォーム（contract）]
      T
      X
      D
      P
    end
```

### 補足
- Chainlink の定期実行基盤が対象時期をトリガ
- コントラクト側で利益分配または満期処理対象を判定
- 必要に応じて管理者が USDT を補充
- USDT プールから各出資者ウォレットへ送付（成功時は即時 Push）
- **送金失敗時**: 利回り・元本をエスクロー計上しバッチは継続。投資家が **`claimYield` / `claimPrincipal`** で請求（現 NFT 保有者のみ）
- **満期処理の場合**、送金成功時は **未請求 yield も含めて合算送金し** NFT を焼却。送金失敗時は元本エスクロー（yield もそのまま）、NFT は burn しない

### UC: エスクロー利回りの請求（投資家）

| 項目 | 内容 |
|------|------|
| 前提 | 分配または満期 Push で `YieldTransferFailed` / `PrincipalTransferFailed` が発生 |
| 主体 | 当該 `tokenId` の `ownerOf` |
| 手順 | マイページで未請求残を確認 → 必要なら NFT をクリーンなウォレットへ移転 → `claimYield`（個別 slot）または `claimPrincipal`（元本 + 残 yield 一括）を実行 |
| 結果 | `claimYield`: 成功時 `YieldClaimed`、失敗時 revert（資金留保）。`claimPrincipal`: 成功時 slot ごとに `YieldClaimed` + `PrincipalClaimed` + burn、失敗時 revert（全て留保） |
| 備考 | `claimPrincipal` は未請求 yield 全 slot を元本と合算して 1 回で送金し burn する（F-2026-17209 対応）|

---

## 5. システム連携

```mermaid
flowchart LR
    A[管理者] --> S[システム連携]
    S --> H[情報取得<br/>（商品情報・NFT発行情報）]
    H -.-> R[各種情報]
    H -.-> S

    subgraph FRONT[フロントシステム]
      S
    end

    subgraph CONTRACT[Web3小口投資プラットフォーム（contract）]
      H
      R
    end
```

### 補足
- 管理者がフロントシステムから情報連携を実行
- コントラクト側から商品情報・NFT発行情報などを取得
- 取得結果を各種情報として参照

---

## 6. 構成まとめ

| 区分 | 主な役割 |
|---|---|
| フロントシステム | 商品画面、申込、DB保存、管理画面 |
| Web3小口投資プラットフォーム | 商品登録、出資申込、NFT発行、利益分配、満期処理 |
| 銀行 | 日本円入金の受け皿 |
| 管理者 | 商品登録、着金確認、USDT補充、運用管理 |
| 出資者 | 日本円/USDTで出資、NFT受領、利益受取 |
| Chainlink | 定期実行トリガ |

---
